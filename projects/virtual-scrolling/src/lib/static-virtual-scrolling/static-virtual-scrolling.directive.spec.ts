/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StaticVirtualScrollingDirective } from './static-virtual-scrolling.directive';

describe('Directive: StaticVirtualScrolling', () => {
  it('should create an instance', () => {
    const directive = new StaticVirtualScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
