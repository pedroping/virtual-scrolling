import { StaticElementDirective } from './static-element.directive';

describe('Directive: StaticElement', () => {
  it('should create an instance', () => {
    const directive = new StaticElementDirective();
    expect(directive).toBeTruthy();
  });
});
